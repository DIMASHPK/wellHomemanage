import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  filterButton: {
    borderRadius: 0,
    borderTopRightRadius: 16,
    minWidth: 160,
    padding: 5,
    height: '100%',
  },
}));
